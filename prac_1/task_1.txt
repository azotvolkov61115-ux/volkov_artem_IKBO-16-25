задание 1:
    grep -o "^[^:]*" /etc/passwd | sort 

задание 2:
    cat /etc/protocols | sort -k2 -nr | head -5 | awk '{print $2, $1}'

задание 3:
    #!/bin/bash

    length=${#1}

    printf '+%*s+\n' $((length + 2)) '' | tr ' ' '-'
    echo "| $1 |"
    printf '+%*s+\n' $((length + 2)) '' | tr ' ' '-'

задание 4:
